#include "iostream"
#include <string>
#include <vector>
using  namespace std;

void main()
{
	int n;
	while (cin >> n)
	{
		if (n == 42)
		{
			break;
		}
	}
	//int *p = &n;
	//cout << &n << endl;
	//bool b = (p != 0 && *p++);

	//int ival = 0;
	//bool c = (ival++ && ival);

	
	system("pause");
}
